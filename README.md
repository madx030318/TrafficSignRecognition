# TrafficSignRecognition
A computer vision project that uses machine learning to recognize and classify traffic signs from images. The system provides a Flask REST API where a client can submit their name and an image URL, after which the image is processed and passed to a trained neural network for traffic sign classification.
